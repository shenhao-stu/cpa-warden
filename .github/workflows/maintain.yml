name: Scheduled Maintenance

on:
  schedule:
    # Run once per day
    - cron: '7 3 * * *'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  maintain:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          ref: web

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install aiohttp rich requests psycopg2-binary

      - name: Run maintenance scan
        id: scan
        env:
          CPA_INSTANCES: ${{ secrets.CPA_INSTANCES }}
          GROK_PG_DSN: ${{ secrets.GROK_PG_DSN }}
          GROK_TOKEN_MAX_AGE_H: "48"
          SUB2API_URL: ${{ secrets.SUB2API_URL }}
          SUB2API_ADMIN_EMAIL: ${{ secrets.SUB2API_ADMIN_EMAIL }}
          SUB2API_ADMIN_PASSWORD: ${{ secrets.SUB2API_ADMIN_PASSWORD }}
        run: python3 .github/scripts/scheduled_maintain.py

      - name: Send Feishu notification
        if: always()
        env:
          FEISHU_WEBHOOK: ${{ secrets.FEISHU_WEBHOOK }}
          SCAN_RESULT: ${{ steps.scan.outputs.result }}
          SCAN_STATUS: ${{ steps.scan.outcome }}
        run: python3 .github/scripts/feishu_notify.py
